{
  "name": "openclaw-app-router-auth",
  "version": "0.1.0",
  "description": "Per-app password auth sidecar for the OpenClaw app router (Caddy forward_auth).",
  "private": true,
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test server.test.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cookie-parser": "^1.4.7",
    "express": "^4.21.2"
  }
}
